README

First pip install < requirements.txt to install the required packages.

Then simply enter <python app.py> in your shell and a local flask server will be started at 127.0.0.1:5000 (Enter this in your browser). This website uses the two functions from scraper.py to either scrape github or use the github API to search all github repositories for the search term you enter in the form.
